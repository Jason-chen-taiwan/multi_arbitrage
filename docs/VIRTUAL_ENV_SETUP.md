# 虛擬環境設置 - 完整指南

## ✅ 已完成

您的 StandX 做市商系統現在已經配備完整的虛擬環境支持！

## 📁 新增文件

1. **setup.sh** - 自動化環境設置腳本
   - 檢測 Python 版本
   - 創建虛擬環境
   - 安裝所有依賴

2. **run.sh** - 便捷啟動腳本
   - 自動激活虛擬環境
   - 提供簡化命令
   - 支持所有功能

3. **QUICKSTART.md** - 更新的快速入門指南
   - 虛擬環境使用說明
   - 命令參考
   - 常見問題解答

## 🚀 立即開始

### 第一步：設置環境

```bash
# 賦予執行權限（只需一次）
chmod +x setup.sh run.sh

# 運行設置腳本
./setup.sh
```

### 第二步：配置

```bash
# 複製環境變數範例
cp .env.example .env

# 編輯配置
nano .env
```

### 第三步：測試

```bash
# 測試 Web Dashboard（推薦）
./run.sh web
```

訪問：http://localhost:8000

### 第四步：啟動

```bash
# 啟動 Uptime Program 做市商
./run.sh start-uptime
```

## 📋 完整命令列表

### 環境管理

| 命令 | 說明 |
|------|------|
| `./run.sh setup` | 首次設置虛擬環境 |
| `./run.sh install` | 安裝/更新依賴 |
| `./run.sh clean` | 清理虛擬環境 |
| `./run.sh shell` | 進入虛擬環境 shell |

### 測試功能

| 命令 | 說明 |
|------|------|
| `./run.sh test` | 終端 Dashboard 測試（60秒）|
| `./run.sh test --duration 120` | 自定義時長測試 |
| `./run.sh web` | Web Dashboard 演示（5分鐘）|
| `./run.sh web --duration 600` | 自定義時長演示 |
| `./run.sh dashboard` | 僅啟動 Dashboard Server |

### 生產運行

| 命令 | 說明 |
|------|------|
| `./run.sh start-uptime` | Uptime Program 策略（推薦）|
| `./run.sh start` | 標準策略 |
| `./run.sh start config/xxx.yaml` | 自定義配置 |

## 🎯 為什麼使用虛擬環境？

### ✅ 優點

1. **依賴隔離**
   - 不影響系統 Python
   - 每個專案獨立環境
   - 避免版本衝突

2. **安全性**
   - 不需要 sudo 權限
   - 降低系統風險
   - 易於清理

3. **可移植性**
   - 易於在其他機器重現
   - 版本控制友好
   - 團隊協作方便

4. **版本管理**
   - 固定依賴版本
   - requirements.txt 鎖定
   - 避免意外更新

### 📊 對比

| 特性 | 全域安裝 | 虛擬環境 |
|------|---------|---------|
| 隔離性 | ❌ | ✅ |
| 安全性 | ⚠️ | ✅ |
| 可移植 | ❌ | ✅ |
| 易清理 | ❌ | ✅ |
| 權限需求 | sudo | 無 |
| 空間佔用 | 小 | ~300MB |

## 🔧 進階使用

### 手動控制虛擬環境

```bash
# 激活
source venv/bin/activate

# 查看已安裝套件
pip list

# 安裝額外套件
pip install some-package

# 退出
deactivate
```

### 更新依賴

```bash
# 方式 1：使用腳本（推薦）
./run.sh install

# 方式 2：手動更新
source venv/bin/activate
pip install --upgrade -r requirements.txt
```

### 添加新依賴

```bash
# 1. 激活環境
source venv/bin/activate

# 2. 安裝套件
pip install new-package

# 3. 更新 requirements.txt
pip freeze > requirements.txt

# 4. 或手動編輯 requirements.txt
nano requirements.txt
```

### 重置環境

```bash
# 清理舊環境
./run.sh clean

# 重新設置
./run.sh setup
```

## 🐛 故障排除

### 問題：找不到 Python

```bash
# macOS 使用 Homebrew 安裝
brew install python@3.11

# 或從 python.org 下載
```

### 問題：權限錯誤

```bash
# 確保腳本有執行權限
chmod +x setup.sh run.sh
```

### 問題：虛擬環境損壞

```bash
# 重新創建
./run.sh clean
./run.sh setup
```

### 問題：依賴安裝失敗

```bash
# 升級 pip
source venv/bin/activate
pip install --upgrade pip

# 重新安裝依賴
pip install -r requirements.txt
```

### 問題：命令找不到

```bash
# 確保在正確目錄
cd /path/to/arbitrage

# 確保腳本存在
ls -la setup.sh run.sh
```

## 📚 文件結構

```
arbitrage/
├── venv/                    # 虛擬環境（自動創建，不提交 Git）
├── setup.sh                 # ⭐ 環境設置腳本
├── run.sh                   # ⭐ 便捷啟動腳本
├── .env                     # 環境變數（手動創建，不提交 Git）
├── .env.example             # 環境變數範例
├── .gitignore               # Git 忽略規則（包含 venv/）
├── requirements.txt         # Python 依賴
├── README.md                # 系統說明（已更新）
├── QUICKSTART.md            # 快速入門（已更新）
├── config/                  # 配置文件
├── src/                     # 源代碼
├── scripts/                 # 運行腳本
└── docs/                    # 文檔
```

## 🎓 學習資源

### 虛擬環境概念

- [Python 官方文檔](https://docs.python.org/3/library/venv.html)
- [Real Python 教程](https://realpython.com/python-virtual-environments-a-primer/)

### 最佳實踐

1. **永遠使用虛擬環境**
   - 每個專案一個環境
   - 不要在系統全域安裝

2. **保持 requirements.txt 更新**
   - 每次添加套件後更新
   - 使用 `pip freeze` 固定版本

3. **不要提交虛擬環境到 Git**
   - `venv/` 應在 `.gitignore` 中
   - 只提交 `requirements.txt`

4. **定期更新依賴**
   - 檢查安全更新
   - 測試兼容性

## ⚡ 快速參考

### 常用命令速查

```bash
# 設置（首次）
./setup.sh

# 測試
./run.sh web

# 啟動
./run.sh start-uptime

# 更新
./run.sh install

# 清理
./run.sh clean
```

### 開發流程

```bash
# 1. 設置環境（只需一次）
./setup.sh

# 2. 配置
cp .env.example .env
nano .env

# 3. 開發測試
./run.sh test
./run.sh web

# 4. 運行
./run.sh start-uptime
```

### 生產部署

```bash
# 1. 克隆專案
git clone <repo-url>
cd arbitrage

# 2. 設置環境
./setup.sh

# 3. 配置
cp .env.example .env
# 編輯 .env

# 4. 測試
./run.sh test

# 5. 啟動
./run.sh start-uptime
```

## 💡 提示與技巧

### 提示 1：快速重啟

```bash
# 殺死進程
Ctrl+C

# 立即重啟
./run.sh start-uptime
```

### 提示 2：查看日誌

```bash
# 運行時查看輸出
./run.sh start-uptime | tee logs/output.log
```

### 提示 3：後台運行

```bash
# 使用 screen 或 tmux
screen -S market_maker
./run.sh start-uptime
# Ctrl+A D 分離

# 重新連接
screen -r market_maker
```

### 提示 4：自動重啟

```bash
# 使用 while 循環
while true; do
    ./run.sh start-uptime
    echo "重啟中..."
    sleep 5
done
```

## 🎉 總結

現在您擁有：

✅ **完整的虛擬環境系統**
- 自動化設置腳本
- 便捷啟動命令
- 依賴隔離

✅ **簡化的工作流程**
- `./setup.sh` - 一鍵設置
- `./run.sh xxx` - 簡化命令
- 自動環境激活

✅ **專業的開發環境**
- 版本控制友好
- 團隊協作方便
- 易於部署

✅ **完整的文檔**
- README.md 更新
- QUICKSTART.md 重寫
- 詳細故障排除

## 🚀 下一步

1. ✅ 運行 `./setup.sh` 設置環境
2. ✅ 配置 `.env` 文件
3. ✅ 測試 `./run.sh web`
4. ✅ 啟動 `./run.sh start-uptime`

開始使用隔離環境運行您的做市商系統！🎯

---

**相關文檔**：
- [README.md](README.md) - 完整系統說明
- [QUICKSTART.md](QUICKSTART.md) - 快速入門指南
- [WEB_DASHBOARD_GUIDE.md](docs/WEB_DASHBOARD_GUIDE.md) - Dashboard 使用
- [UPTIME_PROGRAM_GUIDE.md](docs/UPTIME_PROGRAM_GUIDE.md) - Uptime Program 詳解
